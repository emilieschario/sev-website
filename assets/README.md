Place brand assets for The Floss Drop here.

Recommended filename for the hero image: `dentist-hero.jpg`.

Dimensions: at least 1600px wide (any aspect ratio works). The layout uses `object-fit: cover` to fill the right-side column.

To change the image path, edit `floss-drop.html` and update the `<img>` tag inside the `aside.right` element.


